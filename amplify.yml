version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm --prefix website ci
    build:
      commands:
        - npm --prefix website run build
  artifacts:
    baseDirectory: website/.next
    files:
      - "**/*"
  cache:
    paths:
      - website/node_modules/**/*
